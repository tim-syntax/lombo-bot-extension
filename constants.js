// Bet amounts for each step
export const BET_SEQUENCE = [0.01, 0.02, 0.04, 0.1, 0.2, 0.4, 1, 2, 4, 10, 20, 40];

// Payout sequence: all steps = 2
export const PAYOUT_SEQUENCE = [2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2];

// Win threshold - result >= 2.0 is a win
export const WIN_THRESHOLD = 2.0;

